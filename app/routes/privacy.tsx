import { createRoute } from 'honox/factory'
export default createRoute((c) => c.render(<main className="mx-auto min-h-screen max-w-[760px] px-6 py-24"><a href="/">exchange<span className="text-accent-strong">.io</span></a><h1 className="mt-16 font-display text-6xl font-bold">Privacy.</h1><p className="mt-8 leading-7 text-muted">The API stores privacy-preserving operational telemetry: daily rotating salted IP hashes, route, status, timing, referer domain, and broad user-agent class. Raw IP addresses and user-agent strings are not stored. Public API responses contain rate provenance, not caller identity.</p></main>, { title: 'Privacy — exchange.io', description: 'exchange.io privacy policy.' }))
